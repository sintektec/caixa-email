{
  "//": "MODELO. Copie este arquivo para 'appsettings.Local.json' na mesma pasta e preencha o valor.",
  "//como": "Windows:  copy appsettings.Local.example.json appsettings.Local.json",

  "//porque": "'appsettings.Local.json' está no .gitignore e nunca é enviado ao repositório. O modelo aqui é versionado justamente por não ter segredo nenhum dentro.",

  "OAuth": {
    "Google": {
      "//": "Chave secreta do cliente, obtida em: Console do Google Cloud -> APIs e serviços -> Credenciais -> o cliente OAuth do tipo 'Aplicativo para computador'. Começa com GOCSPX-.",
      "//naoMexer": "O ClientId já está no appsettings.json e não precisa ser repetido aqui — a configuração é sobreposta chave a chave, e o que não estiver neste arquivo continua vindo de lá.",
      "ClientSecret": "COLE-AQUI-A-CHAVE-SECRETA"
    }
  }
}
